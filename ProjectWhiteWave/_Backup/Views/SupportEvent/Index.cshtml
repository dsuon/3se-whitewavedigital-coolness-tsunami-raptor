@model IEnumerable<ProjectWhiteWave.Models.SupportEvent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateOpened)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateClosed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ticket.TicketId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateOpened)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateClosed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ticket.TicketId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SupportEventId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SupportEventId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SupportEventId })
        </td>
    </tr>
}

</table>
